databases:
  - name: ciudadmoto-db
    plan: free
    databaseName: ciudadmoto
    user: crm

services:
  - type: web
    name: ciudadmoto-api
    plan: free
    runtime: node
    rootDir: projects/crm/backend
    buildCommand: npm ci --include=dev && npx prisma generate && npm run build && npx tsc --esModuleInterop --module commonjs --outDir dist --rootDir . prisma/seed.ts --skipLibCheck
    startCommand: npx prisma db push --accept-data-loss && (node dist/prisma/seed.js || echo "Seed skipped") && node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: DATABASE_URL
        fromDatabase:
          name: ciudadmoto-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        value: https://frontend-two-mu-94.vercel.app
